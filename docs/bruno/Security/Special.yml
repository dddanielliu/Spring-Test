info:
  name: Special
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/special"
  auth:
    type: basic
    username: admin
    password: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
