spring:
  application:
    name: springtest
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_DATABASE:spring_test_db}
    username: ${DB_USER:spring_test_user}
    password: ${DB_PASSWORD:spring_test_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
